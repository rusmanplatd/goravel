{{define "roles/create.tmpl"}}
    {{template "layouts/header.tmpl" .}}
    {{template "layouts/nav.tmpl" .}}

    <!-- Page Content -->
    <main class="container py-4">
        <div class="mb-4">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">
                        <a href="/roles?tenant_id={{.tenantID}}" class="text-decoration-none">
                            <i class="fas fa-user-tag me-1"></i>Roles
                        </a>
                    </li>
                    <li class="breadcrumb-item active" aria-current="page">Create Role</li>
                </ol>
            </nav>
        </div>

        <div class="row">
            <div class="col-lg-3">
                <div class="card shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title">Role Information</h5>
                        <p class="card-text text-muted small">
                            Create a new role for the tenant. Roles define what permissions users have within the system.
                        </p>
                    </div>
                </div>
            </div>
            <div class="col-lg-9">
                <div class="card shadow-sm">
                    <div class="card-body">
                        <form method="POST" action="/roles">
                            <input type="hidden" name="tenant_id" value="{{.tenantID}}">
                            
                            {{if .error}}
                            <div class="alert alert-danger d-flex align-items-center" role="alert">
                                <i class="fas fa-exclamation-circle me-2"></i>
                                <div>{{.error}}</div>
                            </div>
                            {{end}}

                            <div class="row g-3">
                                <div class="col-12">
                                    <label for="name" class="form-label">Role Name <span class="text-danger">*</span></label>
                                    <input type="text" name="name" id="name" class="form-control" 
                                           value="{{.name}}" placeholder="Enter role name" required>
                                </div>

                                <div class="col-12">
                                    <label for="description" class="form-label">Description</label>
                                    <textarea name="description" id="description" rows="3" class="form-control" 
                                              placeholder="Enter role description">{{.description}}</textarea>
                                </div>

                                <div class="col-12">
                                    <label for="guard" class="form-label">Guard <span class="text-danger">*</span></label>
                                    <select name="guard" id="guard" class="form-select" required>
                                        <option value="web" {{if eq .guard "web"}}selected{{end}}>Web</option>
                                        <option value="api" {{if eq .guard "api"}}selected{{end}}>API</option>
                                    </select>
                                </div>

                                <div class="col-12">
                                    <div class="form-check">
                                        <input type="checkbox" name="is_active" id="is_active" value="true" 
                                               class="form-check-input" {{if not .is_active}}{{else}}checked{{end}}>
                                        <label for="is_active" class="form-check-label">
                                            Active
                                        </label>
                                    </div>
                                    <div class="form-text">
                                        Active roles can be assigned to users. Inactive roles are disabled.
                                    </div>
                                </div>
                            </div>

                            <div class="d-flex justify-content-end gap-2 mt-4">
                                <a href="/roles?tenant_id={{.tenantID}}" class="btn btn-secondary">Cancel</a>
                                <button type="submit" class="btn btn-success">
                                    <i class="fas fa-save me-2"></i>Create Role
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </main>

    {{template "layouts/footer.tmpl" .}}
{{end}} 